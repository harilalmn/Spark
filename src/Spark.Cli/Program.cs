namespace Spark.Cli;

/// <summary>
/// Entry point for the <c>spark</c> command line.
/// </summary>
/// <remarks>
/// A stub until M2, when <c>spark run</c> becomes the headless proof that a graph
/// evaluates identically outside the desktop app.
/// </remarks>
internal static class Program
{
    private static int Main(string[] args)
    {
        System.Console.WriteLine("spark: no commands yet. See docs/TODO.md for what lands when.");
        return 0;
    }
}
