# Help-renderer golden for 'command-line' - E11-T7. Generated; do not hand-edit.
# Rewrite it with SPARK_UPDATE_GOLDEN=1, then READ THE DIFF before committing it.
#
# This is what HelpMarkdown makes of docs/help/concepts/command-line.md: every
# block in order, and every inline run inside it with its kind and a link's target.
# HelpMarkdown is a deliberate SUBSET of Markdown (ADR-0019), so the reference it has
# to agree with is itself over the documents that exist - not a specification.
#
# A change here is a change to what a reader sees in the help panel. If it is
# intended, the topic or the parser changed and this file follows; if it is not, the
# parser regressed on real input.
Part	Detail
id	concepts.command-line
title	The command line
since	2026.9
nodes	(none)
related	concepts.files, concepts.evaluation, concepts.code-blocks
blocks	118
example	yes
block[0].paragraph	[strong]Status:[text] Current. Describes all seven verbs, which all exist. [strong]Owner:[text] [code]graph-engine[text] [strong]Last updated:[text] 2026-09-15 ([code]E12-T5[text]: [code]spark docs[text], and the seventh verb of seven)
block[1].quote	[strong]Scope.[text] [code]spark.exe[text] ships beside the desktop application and does everything **without opening
block[2].quote	[text]a window**. It is the same engine, the same node library and the same value rendering; what it
block[3].quote	[text]does not have is a canvas — and since [code]render[text] it does not need one to draw, either. **All seven
block[4].quote	[text]planned verbs now exist**, the last of them on 2026-09-15.
block[5].rule	---
block[6].heading	level=2 [text]Why there is a command line at all
block[7].paragraph	[text]A graph is a document and an evaluation is a computation. Both of those claims are easy to make and easy to be wrong about, and the way they stop being claims is that something outside the application can open a file and get the same answer. That is what [code]spark[text] is for: a build that checks graphs, a script that exports a hundred of them, a diff between yesterday's answer and today's.
block[8].paragraph	[text]Everything it prints is designed to be [strong]redirected[text]. Values go to standard output and problems go to standard error, so [code]spark run graph.spark > values.txt[text] captures the answer and still shows you what went wrong on the way.
block[9].rule	---
block[10].heading	level=2 [code]spark run[text] — what did this graph produce?
block[11].code	lines=1
block[12].paragraph	[text]Opens the graph, evaluates it with no window, and prints [strong]what its watch nodes saw[text]. A watch is the user saying [emphasis]this one[text]; a graph of two thousand nodes has two thousand values and almost none of them is the one you wanted.
block[13].code	lines=3
block[14].paragraph	[text]That first line is on standard error, not standard output: a graph with no watches in it ran perfectly well and simply said nothing, which looks identical to a graph that did nothing. With [code]--all[text] the same file prints every node:
block[15].code	lines=4
block[16].paragraph	[text]Values print in the [strong]document's[text] order rather than the graph's, so two runs of one file print the same values in the same order — which is the only reason to print them at all. The rendering is the same one the canvas and the properties pane use, from one shared implementation, so what the command line says and what the application shows cannot drift apart.
block[17].paragraph	[code]--no-script[text] refuses a graph containing a code block instead of running it. It [strong]refuses rather than dropping the executable parts[text], because a graph that silently ran with its code blocks missing would produce a wrong answer quietly, which is worse than an error. See [link->code-blocks.md]code blocks[text].
block[18].heading	level=3 [text]A graph's own packages, and [code]--trust-packages
block[19].paragraph	[text]A graph can carry libraries in a folder beside it named after the file — [code]facade.packages[text] beside [code]facade.spark[text] — and its code blocks compile against them. The window asks before it uses one: it names each assembly and its hash, and [strong]Trust and load[text] remembers the answer against those exact bytes. [code]run[text] and [code]check[text] use [strong]the same record[text], so an assembly you agreed to in Spark is simply used. They have nobody to ask about the rest, so they [strong]refuse the graph and name what they found[text]:
block[20].code	lines=6
block[21].paragraph	[text]The hash is printed in full, where the window shows eight characters, so a build can compare it with the one it expects. [code]--trust-packages[text] uses the folder's assemblies [strong]for that run only[text] and writes nothing down — the record is shared with the desktop application, and a scheduled job should not become your consent in the window. A file that cannot be read is refused even then, because it has no hash to agree to.
block[22].paragraph	[strong]Only a graph with a code block looks at its folder[text], because nothing else compiles against it. A package the file names that is missing from the folder prints as a warning, since it is the reason a block that uses it will not compile; [code]check --strict[text] fails on it.
block[23].paragraph	[text]Why a DLL is asked about when the code blocks are not: the code is text inside the graph, which a reviewer reads in a diff, and an assembly is not.
block[24].rule	---
block[25].heading	level=2 [code]spark check[text] — is this graph broken?
block[26].code	lines=1
block[27].paragraph	[text]The same evaluation with the printing taken away. [strong]It says nothing at all when nothing is wrong[text], and its answer is the exit code:
block[28].table	rows=3 | [text]Exit code ; [text]Meaning | [code]0 ; [text]Every node evaluated. Warnings may have been printed; none of them failed the run. | [code]1 ; [text]A node errored, the file would not open, or the arguments were wrong.
block[29].code	lines=3
block[30].paragraph	[text]and when something is wrong:
block[31].code	lines=4
block[32].paragraph	[strong]Three things about that output are deliberate.
block[33].listitem	[strong]Silence on success.[text] A gate that writes a line every time is a gate whose output stops being
block[34].paragraph	[text]read, and then the one run that had something to say scrolls past with the rest.
block[35].listitem	[strong]The node is named.[text] [code]spark run[text] prints the code and the message; [code]check[text] prints the node as
block[36].paragraph	[text]well, because a build log is read by somebody who was not watching and a diagnostic code with no node attached is a message that costs an hour. The name is the same one the canvas draws.
block[37].listitem	[strong]Every diagnostic is one line.[text] Several exceptions put a newline in their message —
block[38].paragraph	[code]ArgumentOutOfRangeException[text] appends [emphasis]Actual value was 0.[text] on a line of its own — and a diagnostic split in two is one whose second half has lost its file name, its node and its severity. The canvas keeps the break, because it has the room; the command line does not.
block[39].listitem	[strong]Warnings print and do not fail.[text] A per-element replication failure is a warning by
block[40].paragraph	[text]definition — the node produced a value and everything downstream still evaluated — and a gate that refused those is a gate somebody turns off. See [link->evaluation.md]evaluation[text].
block[41].heading	level=3 [code]--strict[text], and the case that makes it necessary
block[42].paragraph	[strong]The warning rule holds when one element of eight fails and also when eight of eight do[text], and the second is very nearly always a broken graph:
block[43].code	lines=4
block[44].paragraph	[text]The [emphasis]same[text] radius of zero on a node that is not replicating is an [strong]error[text] and fails. That asymmetry is real, it follows from what a warning means rather than from an oversight, and it is exactly the kind of thing a gate should not decide on your behalf. So [code]--strict[text] fails on [strong]any[text] diagnostic at all:
block[45].code	lines=4
block[46].paragraph	[text]Use [code]--strict[text] in a build you control and the default when checking graphs other people wrote — a graph that legitimately warns is common, and a gate that has to be argued with gets removed.
block[47].heading	level=3 [text]Using it in a build
block[48].code	lines=1
block[49].paragraph	[text]or over a folder, where the shell's own exit code does the work:
block[50].code	lines=2
block[51].rule	---
block[52].heading	level=2 [code]spark export[text] — write the geometry out
block[53].code	lines=2
block[54].paragraph	[text]Evaluates with no window and writes what it produced. The format comes from the extension.
block[55].code	lines=1
block[56].paragraph	[text]Curves become polylines and surfaces are tessellated, at a tolerance written into the file's own header. [strong]Solids are not tessellated on the way to STEP or IGES[text] — those carry the exact surfaces, which is the entire point of having them. See [link->solids.md]solids[text].
block[57].paragraph	[strong]A graph with a code block exports like any other[text], and whatever geometry the block makes is written with the rest. [code]--no-script[text] and [code]--trust-packages[text] mean exactly what they mean for [code]run[text]: the first refuses such a graph, the second uses the assemblies in its package folder for this one run. [strong]A refused export writes no file[text], so a script that checks for the output file cannot mistake a refusal for an empty graph.
block[58].code	lines=4
block[59].rule	---
block[60].heading	level=2 [code]spark render[text] — a picture of the graph
block[61].code	lines=2
block[62].paragraph	[text]Evaluates with no window and draws what it produced, 1280×720 by default.
block[63].code	lines=2
block[64].paragraph	[strong]It draws with the software rasteriser and never the GPU[text], and that is the whole reason the verb is worth having. GPU output varies by driver, by vendor and by day; this path does not, so the same graph gives the same bytes on a build agent with no display and no driver attached to it. That is what makes [code]spark render[text] usable as a visual check in a build rather than only as a convenience — two runs of one graph produce identical files, and a test asserts it.
block[65].paragraph	[strong]The camera frames the geometry for you.[text] There is no flag for a viewpoint, because the output then depends on the graph alone, which is the property a regression check rests on. [code]--no-grid[text] leaves out the ground grid and the world axes, for a picture of the geometry by itself; the default keeps them, because the picture is standing in for the viewport.
block[66].paragraph	[strong]Exit 2 means the graph ran and drew nothing.[text] A graph of pure arithmetic is not broken — it has no geometry in it — but a build that accepted an empty picture without noticing is exactly the failure a visual check exists to catch, so it is a distinct code rather than success. [strong]The file is still written[text], because opening it is how you find out why.
block[67].code	lines=5
block[68].paragraph	[code]--no-script[text] and [code]--trust-packages[text] mean exactly what they mean for [code]run[text] and [code]export[text]. A name that is not a [code].png[text] is refused rather than written under a lying extension.
block[69].rule	---
block[70].heading	level=2 [code]spark pkg[text] — is this checkout complete?
block[71].code	lines=2
block[72].paragraph	[text]A graph records the packages it expects, and they live in [code]GRAPH.packages[text] beside it. [code]list[text] reconciles the two.
block[73].code	lines=7
block[74].paragraph	[strong]It exits 1 when something is missing[text], and that is the reason to run it rather than to look in the folder. A build that cannot gate here meets the same fact later, as a compile error inside a code block, two steps and one confusing message from the cause.
block[75].paragraph	[strong]`unrecorded` is the third state[text], and it is not a failure. An assembly in the folder that the file does not record loads and works today — and it will not travel with the graph, because nothing tells the next machine to fetch it.
block[76].paragraph	[strong]`restore` fetches what the file records and the folder lacks.
block[77].code	lines=5
block[78].paragraph	[strong]Restoring downloads; it does not agree.[text] A folder of assemblies beside a downloaded graph is remote code execution, which is why nothing loads without consent recorded per content hash — see [link->files.md]saving and opening graphs[text]. A verb that both fetched code and consented to it on your behalf would be a hole in that, so [code]run[text] and [code]check[text] go on refusing assemblies nobody has agreed to, exactly as they did before you restored them.
block[79].paragraph	[strong]A loose `.dll` cannot be restored.[text] It came from no feed, so there is nowhere to fetch it from; it is named, and the run fails, because the folder is still incomplete.
block[80].rule	---
block[81].heading	level=2 [code]spark graph[text] — what is in this file, and will it open here?
block[82].code	lines=1
block[83].paragraph	[text]Every other verb [emphasis]opens[text] the graph: it binds each node to a definition in the library, compiles any code block, and then does its work. [code]spark graph[text] does none of that. It reads the file and describes it.
block[84].code	lines=25
block[85].paragraph	[strong]Because it binds nothing, it is the one verb that still works on a graph this build cannot open[text] — and that is when you want it. A colleague sends you a file, [code]spark check[text] says a node is unresolved, and the question is [emphasis]which node, and what do I need to install[text]:
block[86].code	lines=20
block[87].paragraph	[text]Two lines and you know the answer: install [code]acme.nodes[text] 1.2.0, or [code]spark pkg restore[text].
block[88].paragraph	[strong]`missing` is a definition this build does not have[text], named in full as [code]package/name[text] — the same key the file stores, so it is searchable. [strong]`present` is one it does.[text] [strong]`in file` is a code block[text], which is neither: its definition [emphasis]is[text] its source, carried in the graph, so no library could hold it and it is never reported as missing.
block[89].paragraph	[strong]It exits 1 when anything it names is absent here[text], for [code]spark pkg list[text]'s reason — the exit code is the answer to [emphasis]will this open on this machine[text], which is a thing a build script can act on.
block[90].paragraph	[strong]It never loads the compiler.[text] Describing a graph full of code blocks costs nothing beyond reading the file, because the source is text until something compiles it and this verb never does.
block[91].paragraph	[strong]The output is stable[text]: the definitions are sorted by key, so two runs over one file produce identical bytes and a build log diffs cleanly.
block[92].rule	---
block[93].heading	level=2 [code]spark docs[text] — the help, from a terminal
block[94].code	lines=1
block[95].paragraph	[text]This is the same help the application shows under F1. Not a copy of it, and not a second generator: the concept topics you are reading are files, the node pages are produced from the node library itself, and one piece of code assembles the three for both the window and this verb. A node that exists has a page here; one that does not, does not.
block[96].paragraph	[strong]With no arguments it lists what there is.
block[97].code	lines=8
block[98].paragraph	[strong]`written` and `generated` is the distinction you need[text], because only the written ones are files. A concept topic is something you can edit and send a change to; a node page is produced from the node and there is nothing to edit — if it is wrong, the node's own documentation is wrong.
block[99].paragraph	[strong]`--topic` prints one page as Markdown[text], which is [code]man[text] for a Spark node:
block[100].code	lines=12
block[101].paragraph	[text]Get the id wrong and it says so and suggests, rather than printing nothing and succeeding:
block[102].code	lines=7
block[103].paragraph	[strong]`--out` writes them all out as Markdown files[text] — for publishing, for reading on a machine with no Spark, or for putting a documentation change in a pull request diff:
block[104].code	lines=2
block[105].paragraph	[text]The tree mirrors what a topic id means — kind, then package, then node — so the page above lands at [code]site/nodes/Spark.Nodes.Core/Point.FromCoordinates.md[text]. [strong]Writing twice gives identical bytes[text], so regenerating a published tree produces an empty diff rather than churn, and the files use line feeds on every platform so a tree written on Windows and one written on Linux are the same.
block[106].paragraph	[strong]Nothing is deleted.[text] The directory is created if it is missing and files in it are overwritten, but a topic that no longer exists leaves its old file behind — emptying a directory somebody named is not this verb's business, and a mistyped path would make that failure silent and total.
block[107].rule	---
block[108].heading	level=2 [code]spark --version
block[109].paragraph	[text]Prints the version, and the third-party notice that the licence requires: which kernel is loaded, under what licence, and that it is dynamically linked and replaceable. The same text appears in the application's About box, from one source, because two copies of a licence notice is one copy that stops matching the build.
block[110].rule	---
block[111].heading	level=2 [text]All seven exist
block[112].paragraph	[text]This section listed what was missing from the day the topic was written until [strong]2026-09-15[text], when [code]render[text], [code]pkg[text], [code]graph[text] and [code]docs[text] all landed and emptied it. [code]spark --help[text] names every verb it accepts and accepts every verb it names.
block[113].rule	---
block[114].heading	level=2 [text]Related
block[115].listitem	[link->files.md]Saving and opening graphs[text] — what is in the file these verbs read.
block[116].listitem	[link->evaluation.md]How a graph evaluates[text] — what a diagnostic is, and why errors do not cascade.
block[117].listitem	[link->code-blocks.md]Code blocks[text] — and why [code]--no-script[text] refuses rather than skips.
