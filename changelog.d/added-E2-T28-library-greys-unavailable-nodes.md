The node library greys out solid nodes this build's kernel cannot run and says why, instead of
letting you place one and find out when it fails.

A build with no kernel provider shows every solid node as unavailable with one sentence explaining
it; a graph that already uses those nodes still opens, still shows them, and still saves unchanged.
