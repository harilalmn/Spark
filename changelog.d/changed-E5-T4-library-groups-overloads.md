The node library groups a member's overloads under one row, which opens a list of the ways to call
it, instead of listing each overload as its own entry.

Spark's own node library contains no overloads, so this is visible only with an imported package
that has them.
